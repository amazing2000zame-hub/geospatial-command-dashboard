---
phase: 01-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - docker-compose.yml
  - .env
  - .env.example
  - .gitignore
  - frontend/package.json
  - frontend/vite.config.ts
  - frontend/tsconfig.json
  - frontend/tsconfig.node.json
  - frontend/index.html
  - frontend/src/main.tsx
  - frontend/src/App.tsx
  - frontend/src/App.css
  - frontend/src/components/Globe.tsx
  - frontend/src/components/SearchBar.tsx
  - frontend/src/components/CoordinateDisplay.tsx
  - frontend/src/types/geojson.ts
  - frontend/src/utils/cesiumHelpers.ts
  - nginx/Dockerfile
  - nginx/nginx.conf
  - backend/Dockerfile
  - backend/package.json
  - backend/tsconfig.json
  - backend/src/types/geojson.ts
autonomous: true
requirements:
  - GLOB-01
  - GLOB-02
  - GLOB-03
  - GLOB-06
  - GLOB-07
  - DEPL-01
  - DEPL-02
  - DEPL-04
  - DEPL-05
  - DEPL-06

must_haves:
  truths:
    - "User sees a photorealistic 3D globe with terrain, satellite imagery, day/night cycle, and atmosphere"
    - "User can rotate, zoom, and tilt the globe with smooth mouse/touch controls"
    - "User can fly to a city via the search bar"
    - "User sees lat/lng coordinates updating as cursor moves over globe"
    - "docker compose build completes without errors for all 3 services"
  artifacts:
    - path: "frontend/vite.config.ts"
      provides: "Vite build with vite-plugin-static-copy for CesiumJS assets"
      contains: "viteStaticCopy"
    - path: "frontend/src/components/Globe.tsx"
      provides: "Resium Viewer with terrain, lighting, requestRenderMode"
      contains: "Terrain.fromWorldTerrain"
    - path: "frontend/src/types/geojson.ts"
      provides: "Shared GeoJSON types for all layers"
      exports: ["LayerFeature", "LayerFeatureCollection", "LayerFeatureProperties"]
    - path: "docker-compose.yml"
      provides: "3-service Docker Compose stack"
      contains: "redis"
    - path: "nginx/Dockerfile"
      provides: "Multi-stage build: frontend Vite build then nginx serve"
      contains: "VITE_CESIUM_ION_TOKEN"
    - path: "nginx/nginx.conf"
      provides: "Reverse proxy with WebSocket upgrade headers"
      contains: "proxy_set_header Upgrade"
  key_links:
    - from: "frontend/src/main.tsx"
      to: "frontend/src/App.tsx"
      via: "React root render without StrictMode"
      pattern: "createRoot.*render"
    - from: "nginx/nginx.conf"
      to: "backend:4010"
      via: "upstream proxy_pass for /api/ and /socket.io/"
      pattern: "proxy_pass.*backend"
    - from: "docker-compose.yml"
      to: "nginx/Dockerfile"
      via: "build context with CESIUM_ION_TOKEN build arg"
      pattern: "VITE_CESIUM_ION_TOKEN"
---

<objective>
Scaffold the complete project structure, replace the broken skeleton with the correct stack, configure Docker Compose with 3 services (nginx, backend, redis), set up CesiumJS globe with terrain/lighting/atmosphere, and establish shared types.

Purpose: This is the foundation that Plans 01-02 and 01-03 build upon. Without a correct Vite config, working Docker Compose, proper nginx proxy with WebSocket headers, and a rendering CesiumJS globe, nothing else can proceed.

Output: A buildable Docker Compose stack with a working CesiumJS globe visible at http://192.168.1.65:3010. Backend will be a stub health endpoint. Frontend shows globe with terrain, day/night, search, and coordinate display.
</objective>

<execution_context>
@/root/.claude/get-shit-done/workflows/execute-plan.md
@/root/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/01-foundation/01-RESEARCH.md
</context>

<tasks>

<task type="auto">
  <name>Task 1: Replace skeleton with correct stack - project configs, shared types, Docker infrastructure</name>
  <files>
    docker-compose.yml
    .env
    .env.example
    .gitignore
    frontend/package.json
    frontend/vite.config.ts
    frontend/tsconfig.json
    frontend/tsconfig.node.json
    frontend/index.html
    frontend/src/types/geojson.ts
    frontend/src/utils/cesiumHelpers.ts
    backend/package.json
    backend/tsconfig.json
    backend/Dockerfile
    backend/src/types/geojson.ts
    backend/src/index.ts
    nginx/Dockerfile
    nginx/nginx.conf
  </files>
  <action>
    Replace the broken skeleton project with the correct stack. The existing code has 6 critical issues that must be fixed (see 01-RESEARCH.md "Existing Code Audit" section).

    **1. docker-compose.yml** - Rewrite with exactly 3 services (NOT 4 -- no separate frontend container):
    - `redis`: redis:7.2-alpine, command with `--maxmemory 256mb --maxmemory-policy allkeys-lru --save ""`, healthcheck with redis-cli ping, network: geospatial. No volumes (cache-only).
    - `backend`: build from ./backend, container_name geospatial-backend, env vars NODE_ENV=production PORT=4010 REDIS_URL=redis://redis:6379, depends_on redis (condition: service_healthy), network: geospatial.
    - `nginx`: build context `.` with dockerfile `nginx/Dockerfile`, build arg `VITE_CESIUM_ION_TOKEN=${CESIUM_ION_TOKEN}`, container_name geospatial-nginx, ports 3010:80, depends_on backend, network: geospatial.
    - Remove the old separate `frontend` service entirely. nginx multi-stage build handles frontend.
    - Use `networks: geospatial: driver: bridge`.

    **2. .env** - Update:
    - Keep existing env vars (USGS_EARTHQUAKE_URL, NWS_ALERTS_URL, NWS_USER_AGENT, CESIUM_ION_TOKEN, OPENSKY_*, NASA_*, REDIS_URL)
    - Change PORT from 3001 to 4010
    - Add comment: user MUST set CESIUM_ION_TOKEN (register free at https://cesium.com/ion/)

    **3. .env.example** - Create with all var names but placeholder values. CESIUM_ION_TOKEN=your_token_here. Commit this file.

    **4. .gitignore** - Ensure .env is listed, node_modules/, dist/, build/, *.log, .DS_Store. Remove the `.dockerignore` entry (that should NOT be gitignored).

    **5. frontend/package.json** - Rewrite:
    - Dependencies: cesium@^1.139.0, resium@^1.19.0, react@^19.0.0, react-dom@^19.0.0, socket.io-client@^4.8.3, zustand@^5.0.0
    - DevDependencies: typescript@^5.4.0, vite@^6.0.0, @vitejs/plugin-react, vite-plugin-static-copy, @types/react@^19, @types/react-dom@^19
    - Scripts: dev="vite", build="tsc -b && vite build", preview="vite preview"
    - REMOVE vite-plugin-cesium (abandoned, NEVER use)

    **6. frontend/vite.config.ts** - Rewrite using Pattern 1 from 01-RESEARCH.md:
    - Import viteStaticCopy from 'vite-plugin-static-copy'
    - cesiumSource = 'node_modules/cesium/Build/Cesium'
    - cesiumBaseUrl = 'cesiumStatic'
    - viteStaticCopy targets: ThirdParty, Workers, Assets, Widgets from cesiumSource to cesiumBaseUrl
    - define: { CESIUM_BASE_URL: JSON.stringify(`/${cesiumBaseUrl}`) }
    - optimizeDeps: { exclude: ['cesium'] }
    - server.proxy: /api -> http://localhost:4010, /socket.io -> ws http://localhost:4010
    - build.outDir = 'dist', sourcemap = false

    **7. frontend/tsconfig.json** - Standard React + Vite config with strict mode, ESNext module, jsx react-jsx. Include ["src"]. Must NOT conflict with vite.config.ts types.

    **8. frontend/index.html** - Root HTML with div#root, script type module src /src/main.tsx. Title: "Geospatial Command Dashboard". Viewport meta tag.

    **9. frontend/src/types/geojson.ts** - Create shared GeoJSON types from 01-RESEARCH.md "Unified GeoJSON Type":
    - Export LayerFeatureProperties interface: id, layer, label, timestamp, category, severity, [key: string]: unknown
    - Export LayerFeature interface: type 'Feature', geometry with type Point|Polygon|MultiPolygon and coordinates
    - Export LayerFeatureCollection interface: type 'FeatureCollection', features array, optional metadata (source, fetchedAt, count, nextUpdate)

    **10. frontend/src/utils/cesiumHelpers.ts** - Create utility functions:
    - magnitudeToColor(mag: number): Cesium.Color -- gradient from LIGHTGREEN (<3) to YELLOWGREEN (3+) to YELLOW (4+) to ORANGE (5+) to RED (6+)
    - magnitudeToSize(mag: number): number -- Math.max(5, mag * 3)
    - severityToColor(severity: string): Cesium.Color -- extreme=RED.withAlpha(0.5), severe=ORANGE.withAlpha(0.5), moderate=YELLOW.withAlpha(0.5), minor=LIGHTBLUE.withAlpha(0.4), default=GRAY.withAlpha(0.3)

    **11. backend/package.json** - Rewrite:
    - Dependencies: fastify@^5.8.0, socket.io@^4.8.3, redis@^5.11.0, node-cron@^3.0.0, @fastify/cors@^10.0.0
    - DevDependencies: typescript@^5.4.0, tsx@^4.7.0, @types/node@^22, @types/node-cron
    - Scripts: dev="tsx watch src/index.ts", build="tsc", start="node dist/index.js"
    - REMOVE @fastify/websocket, ws, ioredis, @types/ws

    **12. backend/tsconfig.json** - ESNext module, NodeNext moduleResolution, outDir dist, rootDir src, strict true, esModuleInterop true, resolveJsonModule true, declaration true.

    **13. backend/Dockerfile** - Multi-stage from 01-RESEARCH.md:
    - Stage 1 (builder): node:22-alpine, workdir /app, copy package*.json, npm ci, copy ., npm run build
    - Stage 2: node:22-alpine, workdir /app, copy package*.json, npm ci --omit=dev, copy --from=builder /app/dist ./dist, expose 4010, CMD ["node", "dist/index.js"]

    **14. backend/src/index.ts** - Minimal stub for now (Plan 01-02 replaces this):
    - Import Fastify, create server on port 4010 host 0.0.0.0
    - Register a GET /api/health route returning { status: 'ok', timestamp: Date.now() }
    - Listen and log startup message
    - This stub validates that Docker Compose builds and the nginx proxy works

    **15. backend/src/types/geojson.ts** - Identical copy of frontend/src/types/geojson.ts (same interfaces).

    **16. nginx/Dockerfile** - Multi-stage from 01-RESEARCH.md:
    - Stage 1 (builder): node:22-alpine, workdir /app, copy frontend/package*.json ./, npm ci, copy frontend/ ., ARG VITE_CESIUM_ION_TOKEN, ENV VITE_CESIUM_ION_TOKEN, npm run build
    - Stage 2: nginx:alpine, copy nginx/nginx.conf to /etc/nginx/conf.d/default.conf, copy --from=builder /app/dist /usr/share/nginx/html, expose 80

    **17. nginx/nginx.conf** - Complete rewrite from Pattern 4 in 01-RESEARCH.md:
    - map $http_upgrade $connection_upgrade block
    - upstream backend { server backend:4010; }
    - location / { root /usr/share/nginx/html; try_files $uri $uri/ /index.html; }
    - location /api/ { proxy_pass http://backend/api/; with headers }
    - location /socket.io/ { proxy_pass with Upgrade and Connection headers, proxy_read_timeout 86400s, proxy_buffering off }
    - location /cesiumStatic/ { root /usr/share/nginx/html; expires 30d; Cache-Control public immutable }
    - gzip on for json, javascript, css with min_length 1000

    Delete the old frontend/Dockerfile and frontend/.dockerignore if they exist (nginx handles frontend build now).
  </action>
  <verify>
    <automated>cd /root/geospatial-dashboard && cat docker-compose.yml | grep -c "services:" && cat frontend/package.json | grep -q "vite-plugin-static-copy" && echo "static-copy OK" && cat frontend/package.json | grep -q "cesium.*1.139" && echo "cesium version OK" && cat backend/package.json | grep -q "fastify.*5" && echo "fastify5 OK" && cat backend/package.json | grep -q '"redis"' && echo "node-redis OK" && cat nginx/nginx.conf | grep -q "proxy_set_header Upgrade" && echo "ws-upgrade OK" && cat docker-compose.yml | grep -c "frontend:" | grep -q "0" && echo "no-separate-frontend OK"</automated>
  </verify>
  <done>
    All project configuration files replaced with correct stack. docker-compose.yml has exactly 3 services (redis, backend, nginx). Frontend uses vite-plugin-static-copy (not vite-plugin-cesium). Backend uses fastify 5, socket.io, node-redis v5 (not fastify 4, ws, ioredis). nginx config has WebSocket upgrade headers for /socket.io/ path. Shared GeoJSON types exist in both frontend and backend.
  </done>
</task>

<task type="auto">
  <name>Task 2: CesiumJS globe with terrain, lighting, atmosphere, search, and coordinate display</name>
  <files>
    frontend/src/main.tsx
    frontend/src/App.tsx
    frontend/src/App.css
    frontend/src/components/Globe.tsx
    frontend/src/components/SearchBar.tsx
    frontend/src/components/CoordinateDisplay.tsx
  </files>
  <action>
    Build the CesiumJS globe viewer with all GLOB requirements. Use patterns from 01-RESEARCH.md.

    **1. frontend/src/main.tsx** - Entry point:
    - Import React, ReactDOM, App, cesium CSS ('cesium/Build/Cesium/Widgets/widgets.css')
    - Set Cesium.Ion.defaultAccessToken from import.meta.env.VITE_CESIUM_ION_TOKEN
    - ReactDOM.createRoot(document.getElementById('root')!).render(<App />)
    - Do NOT wrap in React.StrictMode (causes Resium double-initialization, see pitfall in research)

    **2. frontend/src/App.tsx** - Root component:
    - Import Globe, render it full screen
    - Import App.css for global styles
    - For now, just render Globe (Plan 01-03 adds layer components and status bar)

    **3. frontend/src/App.css** - Global styles:
    - html, body, #root: width 100%, height 100%, margin 0, padding 0, overflow hidden
    - .cesium-viewer: width 100% !important, height 100% !important
    - Style for coordinate display: position absolute, bottom 10px, left 10px, background rgba(0,0,0,0.7), color white, padding 4px 8px, border-radius 4px, font-family monospace, font-size 12px, z-index 1000, pointer-events none
    - Style for search bar: position absolute, top 10px, right 10px, z-index 1000

    **4. frontend/src/components/Globe.tsx** - Main globe component using Pattern 2 from 01-RESEARCH.md:
    - Import Viewer from resium, useMemo/useRef/useCallback from react, * as Cesium from cesium
    - Memoize terrain with useMemo: Cesium.Terrain.fromWorldTerrain({ requestVertexNormals: true })
    - Use Resium <Viewer> with props:
      - full={true}
      - terrain={terrain}
      - timeline={false}, animation={false}
      - baseLayerPicker={false}, geocoder={false} (we provide custom search)
      - homeButton={false}, sceneModePicker={false}, navigationHelpButton={false}
      - infoBox={true}, selectionIndicator={true}
    - On viewer ready callback (via ref):
      - viewer.scene.globe.enableLighting = true (GLOB-03: day/night cycle)
      - viewer.scene.requestRenderMode = true (idle CPU savings)
      - viewer.scene.maximumRenderTimeChange = Infinity
      - viewer.scene.fog.enabled = true
      - viewer.scene.skyAtmosphere.show = true (atmosphere)
    - Store viewer ref for child components
    - Render SearchBar and CoordinateDisplay as children, passing viewer ref
    - Export a way for parent/sibling components to access the viewer ref (via a forwarded ref or context -- keep it simple, pass as prop)

    **5. frontend/src/components/SearchBar.tsx** - Fly-to search (GLOB-06):
    - Text input positioned top-right (styled via App.css class)
    - On Enter key: use Cesium.GeocoderService or the Ion geocoder endpoint to find coordinates
    - Alternatively, use viewer.camera.flyTo() with Cesium.Cartesian3.fromDegrees()
    - For simplicity: use the built-in Cesium Geocoder programmatically via Cesium.IonGeocoderService
    - If the input looks like coordinates (e.g., "36.2, -120.3"), parse and flyTo directly
    - If it looks like a city name, use the Ion geocoder service to resolve, then flyTo
    - Show a loading state while geocoding
    - Clear input on successful fly-to

    **6. frontend/src/components/CoordinateDisplay.tsx** - Cursor coordinates (GLOB-07):
    - Position absolute bottom-left overlay (styled via App.css class)
    - Use ScreenSpaceEventHandler on MOUSE_MOVE
    - Convert screen position to cartographic coordinates via viewer.scene.globe.pick() or viewer.camera.pickEllipsoid()
    - Display latitude and longitude formatted to 4 decimal places
    - Show "---" when cursor is not over the globe
    - Clean up event handler on unmount
    - Call viewer.scene.requestRender() is NOT needed for mouse move (scene renders on interaction)
  </action>
  <verify>
    <automated>cd /root/geospatial-dashboard && cat frontend/src/main.tsx | grep -q "StrictMode" && echo "FAIL: StrictMode found" || echo "StrictMode removed OK" && cat frontend/src/components/Globe.tsx | grep -q "Terrain.fromWorldTerrain" && echo "terrain OK" && cat frontend/src/components/Globe.tsx | grep -q "requestRenderMode" && echo "renderMode OK" && cat frontend/src/components/Globe.tsx | grep -q "enableLighting" && echo "lighting OK"</automated>
  </verify>
  <done>
    Globe component renders CesiumJS viewer with Cesium Ion terrain, satellite imagery, day/night lighting cycle, atmosphere, and requestRenderMode for idle CPU savings. SearchBar allows flying to cities or coordinates. CoordinateDisplay shows lat/lng on cursor move. No StrictMode wrapping Viewer. All CesiumJS objects memoized with useMemo.
  </done>
</task>

<task type="auto">
  <name>Task 3: Build Docker images and verify globe renders</name>
  <files>
    (no new files -- builds and tests what Tasks 1-2 created)
  </files>
  <action>
    Install dependencies and verify the Docker Compose stack builds successfully.

    **1. Install frontend dependencies:**
    ```bash
    cd /root/geospatial-dashboard/frontend && npm install
    ```
    Verify no npm errors. Verify node_modules/cesium/Build/Cesium/ directory exists with Workers, Assets, ThirdParty, Widgets subdirectories.

    **2. Install backend dependencies:**
    ```bash
    cd /root/geospatial-dashboard/backend && npm install
    ```
    Verify no npm errors.

    **3. Run TypeScript compilation checks:**
    ```bash
    cd /root/geospatial-dashboard/frontend && npx tsc --noEmit
    cd /root/geospatial-dashboard/backend && npx tsc --noEmit
    ```
    Fix any type errors. Common issues:
    - Cesium types may need `/// <reference types="cesium" />` or proper tsconfig paths
    - Ensure import.meta.env types are declared (add env.d.ts if needed)

    **4. Verify Vite build works locally:**
    ```bash
    cd /root/geospatial-dashboard/frontend && npx vite build
    ```
    Check that dist/ directory contains cesiumStatic/ with Workers, Assets, ThirdParty, Widgets.
    Check that dist/index.html exists.

    **5. Build Docker Compose stack:**
    ```bash
    cd /root/geospatial-dashboard && docker compose build
    ```
    All 3 services (redis uses pre-built image, backend and nginx build from Dockerfiles) must build without errors.

    **6. Test backend health endpoint:**
    ```bash
    cd /root/geospatial-dashboard && docker compose up -d
    # Wait for services to start
    sleep 10
    curl -s http://localhost:3010/api/health
    ```
    Should return JSON: { "status": "ok", "timestamp": ... }

    **7. Verify nginx serves frontend:**
    ```bash
    curl -s -o /dev/null -w "%{http_code}" http://localhost:3010/
    ```
    Should return 200.

    **8. Verify CesiumJS static assets are served:**
    ```bash
    curl -s -o /dev/null -w "%{http_code}" http://localhost:3010/cesiumStatic/Workers/cesiumWorkerBootstrapper.js
    ```
    Should return 200.

    **9. Stop stack after verification:**
    ```bash
    cd /root/geospatial-dashboard && docker compose down
    ```

    Fix any build failures, missing files, or nginx routing issues before marking done. If CESIUM_ION_TOKEN is empty in .env, the globe will show a low-res ellipsoid -- this is expected until user sets the token. The build itself should still succeed.
  </action>
  <verify>
    <automated>cd /root/geospatial-dashboard && docker compose build 2>&1 | tail -5 && docker compose up -d && sleep 15 && curl -sf http://localhost:3010/api/health && curl -sf -o /dev/null -w "%{http_code}" http://localhost:3010/ && docker compose down</automated>
  </verify>
  <done>
    docker compose build succeeds for all 3 services. docker compose up starts redis, backend, and nginx within 30 seconds. http://localhost:3010/api/health returns 200 with JSON status. http://localhost:3010/ returns 200 and serves the frontend SPA. CesiumJS static assets are accessible via /cesiumStatic/ path. Stack can be stopped cleanly with docker compose down.
  </done>
</task>

</tasks>

<verification>
1. `docker compose build` completes without errors for redis, backend, nginx services
2. `docker compose up -d` starts all 3 services, all reach healthy state within 30 seconds
3. `curl http://localhost:3010/api/health` returns `{"status":"ok",...}`
4. `curl http://localhost:3010/` returns 200 (frontend SPA)
5. `curl http://localhost:3010/cesiumStatic/Workers/cesiumWorkerBootstrapper.js` returns 200
6. Frontend package.json contains vite-plugin-static-copy (NOT vite-plugin-cesium)
7. Backend package.json contains fastify@^5, socket.io@^4, redis@^5 (NOT fastify@^4, ws, ioredis)
8. nginx.conf has /socket.io/ location with Upgrade headers
9. No React StrictMode wrapping the Viewer
10. Globe.tsx uses Terrain.fromWorldTerrain() (NOT createWorldTerrainAsync)
</verification>

<success_criteria>
- Docker Compose builds and runs all 3 services without errors
- Frontend SPA loads at port 3010 through nginx reverse proxy
- Backend health endpoint accessible at /api/health through nginx proxy
- CesiumJS static assets served correctly from /cesiumStatic/
- Globe component configured with terrain, day/night lighting, atmosphere, requestRenderMode
- Search bar and coordinate display components created
- Shared GeoJSON types established in both frontend and backend
- All skeleton code issues from research audit resolved (6/6 fixes applied)
</success_criteria>

<output>
After completion, create `.planning/phases/01-foundation/01-01-SUMMARY.md`
</output>
